<?php
// Defining constants in php here.
define("ROOT_PATH", realpath(dirname(__FILE__))); //This will return the path to root folder using php function
// var_dump(ROOT_PATH);
define("BASE_URL", "http://localhost/FYP_Portal_Updated/blog");//This basically works for assets and links
// This above is with the browser deals with the things that the browser deals the images links to the files and css It willl always be for files and assets
// The global variable to provide the root path so that 
// we don't need to step out in of the folder will be complicated
// This varialble will always point to the root folder of the application.

 //display the path
 //var_dump(ROOT_PATH);
